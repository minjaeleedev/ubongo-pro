namespace Ubongo.Domain
{
    /// <summary>
    /// High-level gameplay mode selected before a run starts.
    /// </summary>
    public enum GameMode
    {
        Classic,
        TimeAttack,
        Zen,
        Multiplayer
    }
}
