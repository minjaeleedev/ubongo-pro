namespace Ubongo.Domain
{
    /// <summary>
    /// Difficulty tier shared by gameplay, UI, and generation rules.
    /// </summary>
    public enum DifficultyLevel
    {
        Easy = 1,
        Hard = 2
    }
}
